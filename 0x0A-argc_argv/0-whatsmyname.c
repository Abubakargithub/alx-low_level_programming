#include <stdio.h>
/**
 * main - main function
 * @argc: argc
 * @argv: argv
 * Return: success always
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
